from scheduler.MOT.schedulerInterface import MOT

# Class implements the MOT interface from schedulerInterface.py
class MOTGeneticAlgorithm(MOT):

	# usefulTime is an integer used by the Hill Climber can ignore it in your implementation
	def find(self, missions, usefulTime):
		pass